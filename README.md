# url_shortner

very basic url shortner using express, mongoose, joi and shortid npm packages.

it also has custom error handler and response handler.

for future if time allows, add testcases and cors and update api and delete api.
